name: "Spell Check"

on:
  push:
    branches:
      - main
    tags: ['*']
  pull_request:

jobs:
  typos-check:
    name: "Spell Check"
    uses: "SciML/.github/.github/workflows/spellcheck.yml@v1"
    secrets: "inherit"
